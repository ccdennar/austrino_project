{
  "name": "@austrino/types",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
  "build": "tsc --project tsconfig.json --outDir dist --emitDeclarationOnly",
  "dev": "tsc --project tsconfig.json --outDir dist --emitDeclarationOnly --watch"
},
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}